# Orbit-7 H3 prompt

Connect the clean images in H1 → H7 order as `<Picture 1>` through `<Picture 7>`. Connect the continuous sweep MP4 as `<Video 1>`.

```text
subject_definitions:
<Subject 1> is one fixed abandoned workshop environment defined jointly by
<Picture 1> through <Picture 7>. The pictures are sequential clockwise views
from one camera center at 60-degree intervals. <Picture 7> returns to exactly
the same physical view as <Picture 1>, closing a full 360-degree loop.
<Video 1> provides clockwise camera timing, angular speed, stabilization, and
loop timing only. Do not copy its compression artifacts.

summary:
[reference generation] Generate one continuous fifteen-second clockwise
360-degree camera rotation inside <Subject 1>, beginning at <Picture 1>, moving
through the regions shown by <Picture 2> through <Picture 6>, and arriving at
<Picture 7>, which is the same view as the start.

retention_analysis:
<Subject 1>: fully_preserved - retain the exact concrete structure, openings,
graffiti, foliage, floor debris, dimensions, lighting, and persistent landmarks
across the complete rotation.

detailed_description:
[Shot 1, 0-15 seconds] One unbroken stabilized chest-height camera rotates
clockwise at constant speed around a fixed center. The environment remains one
physical space. Each supplied view is reached in sequence without a cut. Edge
features pass naturally behind the camera instead of changing identity. At the
end, the view aligns with the starting composition for a seamless loop. No
camera translation, cuts, zooms, teleportation, scene replacement, geometry
morphing, duplicated landmarks, moving walls, people, new objects, label text,
numbers, captions, or watermarks. Quiet natural room ambience only.
```
